<%= render 'users/nav' %>

<div class="circles">
	<div class="row">
		<%= link_to practice_algebras_path(session['user_id']) do %>
			<div class="subject-circle col-md-6">
				<div class="subject">Practice Problems</div>
			</div>
		<% end %>

		<%= link_to quiz_algebras_path(session['user_id']) do %>
			<div class="subject-circle col-md-6">
				<div class="subject">Quiz</div>
			</div>
		<% end %>
	</div>
</div>